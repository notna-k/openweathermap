export enum AvailableForecastParts {
  current = 'current',
  hourly = 'hourly',
  daily = 'daily',
}

export enum ForecastParts {
  current = 'current',
  minutely = 'minutely',
  hourly = 'hourly',
  daily = 'daily',
  alerts = 'alerts',
}
